# CarHub Environment Configuration

# Security
SECRET_KEY=a1d58d4a3d48595ea9dc9665ae2440b4b9496b562d21fe1ab4006c3bf8ca7906

# Email Configuration (Optional - for password reset emails)
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USERNAME=lazerviji80@student.sfit.ac.in
MAIL_PASSWORD=your-gmail-app-password-here
MAIL_DEFAULT_SENDER=lazerviji80@student.sfit.ac.in

# Google OAuth Configuration (Optional)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# OpenAI Configuration (Required for Chatbot)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key-here

# Database Configuration (Optional - uses SQLite by default)
DATABASE_URL=sqlite:///carhub.db

# IMPORTANT SECURITY NOTES:
# 1. Never commit the .env file to version control
# 2. For Gmail, use App Password (not regular password)
# 3. Enable 2-factor authentication first
# 4. Generate App Password in Google Account settings

# To generate Gmail App Password:
# 1. Go to Google Account settings
# 2. Security > 2-Step Verification (enable if not already)
# 3. Security > App passwords
# 4. Generate password for "Mail"
# 5. Use this generated password in MAIL_PASSWORD above

# To get OpenAI API Key:
# 1. Go to https://platform.openai.com/
# 2. Sign up or log in
# 3. Go to API Keys section
# 4. Create a new secret key
# 5. Copy and paste it in OPENAI_API_KEY above
